# V32 Maraton — C(32,8,4) Rekor Koşusu Uygulama Planı

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** C(32,8,4) covering design hücresinde (arşiv 620, bizde 1016) AlphaEvolve-ölçekli bir gece koşusuyla rekor denemek; öncesinde 2026-08-10 oturumunun tespit ettiği üç metodolojik açığı kapatmak (fitness gürültüsü, tek-motor, kısa değerlendirme bütçesi).

**Architecture:** Mevcut discovery-harness P5 hattı üzerine üç ekleme: (1) adaptöre çok-seed ortalamalı değerlendirme (`DISCOVERY_EVAL_SEEDS`), (2) genom sözleşmesine iş-sayaçlı determinizm kuralı, (3) GLM 0.8 + Opus 0.2 ağırlıklı LLM ansamblı. Sonra 300 sn bütçeli tek-hücre (v32) gece koşusu.

**Tech Stack:** Python 3.14, pytest, OpenEvolve 0.3.2, cli-proxy-api (8317 Claude OAuth / 8318 GLM), PowerShell run.ps1.

---

## Okumadan başlama (bağlam)

1. `C:\kt\discovery-harness\CLAUDE.md` **Faz P5 bölümünün tamamı** — dünkü 8 dilimin kaydı ve dersleri orada. Özellikle: 56-piyango olayı (fitness gürültüsü), 35k kod sınırı, arşivleme kancası, `-SolverTimeoutS` parametresi.
2. Bu plan İKİ kabulle yazıldı: (a) makinede başka CPU-yoğun iş koşmuyor; (b) 8317/8318 proxy'leri ayakta (`curl -s -o /dev/null -w "%{http_code}" http://localhost:8317/v1/models` → 401 beklenir; 8318 aynı).
3. Guardrail'ler aynen geçerli: pytest kapısı kırmızıysa döngü başlamaz; referans/arşiv değerleri prompt'a SIZDIRILMAZ (620 sayısı config'e yazılmaz — yalnız `DISCOVERY_ARCHIVE_BELOW` env'inde eşik olarak kullanılır, o solver'a gitmez); evaluator bug şüphesinde döngü durur.

---

### Görev 1: Çok-seed değerlendirme — runner'a extra_args

**Files:**
- Modify: `harness/runner.py` (run_candidate imzası)
- Test: `tests/test_runner.py` (mevcut dosyaya ekle)

- [ ] **Step 1: Failing test yaz** — `tests/test_runner.py` sonuna ekle:

```python
def test_extra_args_passed_to_candidate(tmp_path):
    prog = tmp_path / "echoarg.py"
    prog.write_text(
        "import sys\n"
        "with open(sys.argv[2], 'w') as f:\n"
        "    f.write(' '.join(sys.argv[3:]))\n",
        encoding="utf-8")
    inst = tmp_path / "dummy.txt"
    inst.write_text("x", encoding="utf-8")
    from harness.runner import run_candidate
    rr = run_candidate(prog, inst, timeout_s=15,
                       extra_args=["--seed", "7"])
    assert rr.solution_text == "--seed 7"
```

- [ ] **Step 2: Koş, FAIL gör:** `python -m pytest tests/test_runner.py -q` → `TypeError: ... unexpected keyword argument 'extra_args'`

- [ ] **Step 3: Minimal implementasyon** — `harness/runner.py` içinde `run_candidate` imzasını ve subprocess çağrısını değiştir:

```python
def run_candidate(program_path, instance_path, timeout_s,
                  extra_args=None) -> RunResult:
```

ve `subprocess.run` listesini:

```python
            proc = subprocess.run(
                [sys.executable, str(program), str(instance_path),
                 str(out_path)] + [str(a) for a in (extra_args or [])],
                capture_output=True, text=True, timeout=timeout_s,
                cwd=tmpdir,
            )
```

- [ ] **Step 4: Testler yeşil:** `python -m pytest tests/test_runner.py -q`
- [ ] **Step 5: Commit:** `git add -A && git commit -m "feat(runner): opsiyonel extra_args (cok-seed degerlendirme icin)"`

---

### Görev 2: Çok-seed değerlendirme — adaptörde DISCOVERY_EVAL_SEEDS

**Files:**
- Modify: `harness/evolve_evaluator.py` (`_evaluate_one` + docstring env listesi)
- Test: `tests/test_evolve_evaluator_seeds.py` (yeni)

Anlam: `DISCOVERY_EVAL_SEEDS="0,1"` → aday HER instance için seed başına bir kez koşar; `combined_score` = seed'lerin ortalaması, `cost` = en iyi (SENSE'e göre) koşunun cost'u, `feasible` = min. Böylece fitness "program"ı ölçer, tek koşunun makine-anı piyangosunu değil (2026-08-10 56-piyango dersi). Boş/tanımsız env = mevcut davranış (tek koşu, arg'sız) — mevcut testler bozulmamalı.

- [ ] **Step 1: Failing test yaz** — `tests/test_evolve_evaluator_seeds.py` (yeni dosya):

```python
"""DISCOVERY_EVAL_SEEDS coklu-seed ortalama testleri."""
import textwrap
from pathlib import Path

import pytest

from harness.evolve_evaluator import evaluate


@pytest.fixture
def covering_env(monkeypatch, tmp_path):
    inst = tmp_path / "cover-v7-k3-t2.cover"
    inst.write_text("v 7\nk 3\nt 2\n", encoding="utf-8")
    monkeypatch.setenv("DISCOVERY_PROBLEM", "covering")
    monkeypatch.setenv("DISCOVERY_INSTANCE", str(inst))
    monkeypatch.setenv("DISCOVERY_SOLVER_TIMEOUT_S", "20")
    return tmp_path


FANO = "1 2 4\\n1 3 5\\n2 3 6\\n1 6 7\\n2 5 7\\n3 4 7\\n4 5 6\\n"


def seed_sensitive_program(tmp_path):
    # seed 0 -> optimal 7 blok (fitness 1.0); baska seed -> 8 blok (7/8)
    f = tmp_path / "cand.py"
    f.write_text(textwrap.dedent(f"""\
        import sys
        seed = "0"
        if "--seed" in sys.argv:
            seed = sys.argv[sys.argv.index("--seed") + 1]
        sol = "{FANO}"
        if seed != "0":
            sol += "1 2 3\\n"
        with open(sys.argv[2], "w") as out:
            out.write(sol)
    """), encoding="utf-8")
    return f


def metrics_of(result):
    return result.metrics if hasattr(result, "metrics") else result


def test_mean_over_seeds(covering_env, monkeypatch):
    monkeypatch.setenv("DISCOVERY_EVAL_SEEDS", "0,1")
    m = metrics_of(evaluate(seed_sensitive_program(covering_env)))
    assert m["combined_score"] == pytest.approx((1.0 + 7/8) / 2)
    assert m["cost"] == 7.0          # en iyi kosunun cost'u
    assert m["feasible"] == 1.0


def test_no_env_single_run_no_args(covering_env):
    m = metrics_of(evaluate(seed_sensitive_program(covering_env)))
    assert m["combined_score"] == pytest.approx(1.0)  # arg'siz -> seed 0 yolu
```

- [ ] **Step 2: Koş, FAIL gör:** `python -m pytest tests/test_evolve_evaluator_seeds.py -q`

- [ ] **Step 3: İmplementasyon** — `harness/evolve_evaluator.py` içinde `_evaluate_one`'ı çok-seed'e çevir. Mevcut gövdeyi şu şekle getir (verdict/artifact üretimini seed döngüsüne al):

```python
def _evaluate_one(problem, instance_path, program_path, timeout_s):
    """Tek instance icin (metrics, artifacts); DISCOVERY_EVAL_SEEDS
    varsa seed basina bir kosu, fitness = ortalama (56-piyango dersi:
    tek kosunun makine-ani sansini degil programi olc)."""
    instance = problem.parse_instance(instance_path)
    seeds_env = os.environ.get("DISCOVERY_EVAL_SEEDS", "").strip()
    seed_list = [s.strip() for s in seeds_env.split(",") if s.strip()] \
        if seeds_env else [None]

    artifacts = {}
    runs = []
    for seed in seed_list:
        extra = ["--seed", seed] if seed is not None else None
        rr = run_candidate(program_path, instance_path, timeout_s,
                           extra_args=extra)
        tag = f"seed{seed}:" if seed is not None else ""
        if rr.timed_out:
            artifacts[f"{tag}failure"] = (
                f"timeout: {timeout_s:.0f} sn duvar saati asildi")
        elif rr.returncode != 0:
            artifacts[f"{tag}failure"] = f"solver exit code {rr.returncode}"
        if rr.stderr_tail:
            artifacts[f"{tag}stderr"] = rr.stderr_tail
        verdict = problem.evaluate_text(instance, rr.solution_text)
        _archive_solution(instance_path, rr.solution_text, verdict)
        if not verdict["feasible"]:
            artifacts[f"{tag}violations"] = json.dumps(verdict["violations"])
        runs.append((verdict, rr))

    best_v, best_rr = max(
        runs, key=lambda vr: combined_score(vr[0]["fitness"], problem.sense))
    metrics = {
        "combined_score": sum(
            combined_score(v["fitness"], problem.sense)
            for v, _ in runs) / len(runs),
        "cost": float(best_v["cost"]),
        "feasible": min(1.0 if v["feasible"] else 0.0 for v, _ in runs),
        "solver_s": round(sum(rr.wall_s for _, rr in runs), 3),
        "eval_ms": float(sum(v["eval_ms"] for v, _ in runs)),
    }
    return metrics, artifacts
```

Docstring'deki env listesine ekle: `DISCOVERY_EVAL_SEEDS  virgullu seed listesi ("0,1"); bossa tek kosu`.

- [ ] **Step 4: TÜM adaptör testleri yeşil:** `python -m pytest tests/test_evolve_evaluator_seeds.py tests/test_evolve_evaluator.py tests/test_evolve_evaluator_archive.py -q` (mevcutlar bozulmamalı — bozulursa `[None]` yolunun arg'sız tek koşu olduğunu kontrol et)
- [ ] **Step 5: Commit:** `git commit -am "feat(adapter): DISCOVERY_EVAL_SEEDS cok-seed ortalamali fitness"`

---

### Görev 3: run.ps1 — ikinci proxy anahtarı (ansambl için)

**Files:**
- Modify: `evolve/run.ps1` (api key bölümü)

Ansambl iki api_base kullanır (8318 GLM + 8317 Opus) → iki ayrı key gerekir. OpenEvolve per-model `api_key: "${VAR}"` env çözümlemesini destekler.

- [ ] **Step 1:** `evolve/run.ps1`'de mevcut key-okuma bloğunun ("api-keys bulunamadı" throw'unun) HEMEN ALTINA ekle:

```powershell
# Ansambl icin ikinci anahtar: 8317 (Claude OAuth) config.yaml'dan.
# Yalniz ansambl configleri ${OPENAI_API_KEY_OPUS} kullanir; digerleri etkilenmez.
$opusCfgPath = Join-Path "C:\kt\upwork\cli-api" "config.yaml"
if (Test-Path $opusCfgPath) {
    $opusCfg = Get-Content $opusCfgPath -Raw
    if ($opusCfg -match '(?ms)api-keys:\s*\r?\n\s*-\s*"([^"]+)"') {
        $env:OPENAI_API_KEY_OPUS = $Matches[1]
    }
}
```

- [ ] **Step 2: Sözdizimi kontrolü:** `powershell -Command "Get-Command -Syntax .\evolve\run.ps1"` hatasız dönmeli (paramlar listelenir).
- [ ] **Step 3: Commit:** `git commit -am "feat(run.ps1): OPENAI_API_KEY_OPUS ikinci proxy anahtari"`

---

### Görev 4: v32 ansambl config'i

**Files:**
- Create: `evolve/config.covering.v32.yaml`

- [ ] **Step 1:** `evolve/config.covering.v28.opus.yaml`'ı KOPYALA → `evolve/config.covering.v32.yaml`, sonra şu bölümleri değiştir (dosyanın kalanı — kontrat metni, violation açıklamaları — aynen kalır):

Başlık yorumu:
```yaml
# OpenEvolve konfigürasyonu — P5 covering V32 MARATON (2026-08-11 plani)
# Tek hucre C(32,8,4); ansambl GLM 0.8 + Opus 0.2 (AlphaEvolve Flash/Pro deseni).
# Fitness = 2-seed ortalamasi (DISCOVERY_EVAL_SEEDS="0,1" — run recetesinde).
# Butce 300 sn (COVERING_SEED_TIME_S=300), runner 320 (-SolverTimeoutS 320).
```

`llm:` bloğu (tamamı bununla değişir):
```yaml
llm:
  api_base: "http://localhost:8318/v1"     # varsayilan (GLM)
  api_key: "${OPENAI_API_KEY}"
  models:
    - name: "glm-5"
      weight: 0.8
    - name: "claude-opus-5"
      weight: 0.2
      api_base: "http://localhost:8317/v1"
      api_key: "${OPENAI_API_KEY_OPUS}"
  temperature: 0.8
  max_tokens: 20000
  timeout: 600
  retries: 3
```

`diff_based_evolution: true` yap (çoğunluk GLM; Opus azınlık çağrıları diff'te kesilirse artifact'ta görünür — duman testinde ölç, %40 üstü kayıpta Opus'u ayrı full-rewrite dilimine ayır).

Kaynak/süre metinlerini güncelle: system_message içinde `70 s in this run` → `300 s in this run`; `80 s wall clock` → `320 s wall clock`. Determinizm maddesini ŞUNUNLA DEĞİŞTİR:

```
    - Determinism: accept an optional `--seed N` CLI argument (default 0).
      Same input AND same seed MUST produce the same output. Derive ALL
      phase budgets from WORK COUNTERS (iteration counts, node counts),
      NOT from wall-clock fractions — wall-clock may only be used as the
      final hard stop. Your score is the MEAN over multiple --seed runs,
      so a solver that is only occasionally lucky scores poorly.
```

SCALE bölümünü ŞUNUNLA DEĞİŞTİR:

```
    SCALE: this run targets a SINGLE cell — v=32, k=8, t=4 (C(32,4)=35960
    t-subsets; each block covers C(8,4)=70). Good coverings run to many
    hundreds of blocks, so a cyclic skeleton needs on the order of 20-30
    base-block orbits (one orbit = at most 32 blocks). The 300 s budget
    is there to be USED: deep orbit-set search, aggressive endgame
    (delete a block, repair with multi-block chains), restarts.
```

`evaluator:` bloğu:
```yaml
evaluator:
  timeout: 1400                # 2 seed x 320 sn + dogrulama + tampon
  cascade_evaluation: false
  parallel_evaluations: 4      # cekirdek sayisi el veriyor; LLM gecikmesiyle ortusur
  memory_limit_mb: 2048
  enable_artifacts: true
```

- [ ] **Step 2: YAML geçerliliği:** `python -c "import yaml; yaml.safe_load(open('evolve/config.covering.v32.yaml', encoding='utf-8')); print('ok')"`
- [ ] **Step 3: Sızıntı taraması:** `grep -n "620\|1016" evolve/config.covering.v32.yaml` → HİÇ sonuç dönmemeli (arşiv değeri sızıntısı yasak).
- [ ] **Step 4: Commit:** `git commit -am "feat(evolve): v32 maraton ansambl config"`

---

### Görev 5: Duman testi (5 iter) — ansambl + çok-seed uçtan uca

- [ ] **Step 1: LLM'siz adaptör kontrolü** (kredi yakmadan; ~20 dk çünkü 2 seed × 300 sn):

```bash
cd /c/kt/discovery-harness && DISCOVERY_PROBLEM=covering COVERING_SEED_TIME_S=300 DISCOVERY_SOLVER_TIMEOUT_S=320 DISCOVERY_EVAL_SEEDS="0,1" DISCOVERY_INSTANCE="data/covering/instances/cover-v32-k8-t4.cover" python harness/evolve_evaluator.py evolve/artifacts/best_covering_gen200_20260810.py
```

Beklenen: JSON metrics; `feasible: 1.0`; `solver_s` ≈ 600. `combined_score` not al — gece koşusunun yenmesi gereken taban bu.

- [ ] **Step 2: Duman (5 iter, PowerShell):**

```powershell
Set-Location C:\kt\discovery-harness
$env:COVERING_SEED_TIME_S="300"
$env:DISCOVERY_EVAL_SEEDS="0,1"
$env:DISCOVERY_ARCHIVE_DIR="C:\kt\discovery-harness\runs\evolve\covering-v32-maraton\archive"
$env:DISCOVERY_ARCHIVE_BELOW="700"
.\evolve\run.ps1 -GeceKosusuBitti -Problem covering -Iterations 5 -SolverTimeoutS 320 `
  -ConfigPath evolve\config.covering.v32.yaml -ProxyConfig config.glm.yaml `
  -InitialProgram evolve\artifacts\best_covering_gen200_20260810.py `
  -Instance "data\covering\instances\cover-v32-k8-t4.cover" `
  -OutDir runs\evolve\covering-v32-maraton
```

(`BELOW=700`: 620 rekor eşiği DEĞİL — sadece "iyi çözümleri sakla" filtresi; 620 sayısı hiçbir çıktıya/prompt'a yazılmaz.)

- [ ] **Step 3: Duman analizi:** log'da (a) 5/5 iterasyonda geçerli kod mu, (b) HEM glm-5 HEM claude-opus-5 çağrısı var mı (yoksa per-model api_base çalışmıyor demektir → FALLBACK: `models:` listesinden Opus'u sil, tek-motor GLM ile devam; Opus ayrı dilim olarak kalır), (c) iterasyon başına süre. Kullanıcıya maliyet/süre tablosuyla RAPOR ET; gece koşusu onayını AL.

---

### Görev 6: Gece koşusu (300 iter) — KULLANICI ONAYINDAN SONRA

- [ ] **Step 1:** Duman komutuyla AYNI env + `-Iterations 300` + arka planda başlat. Kaba süre: iterasyon ~5-8 dk (2 seed × 300 sn solver, parallel 4 ile LLM örtüşür) → 300 iter ≈ **12-24 saat**. GLM haftalık kredi pacing'i: ~270 GLM çağrısı — kullanıcı onayında açıkça söyle.
- [ ] **Step 2:** Bitince analiz: yeni en iyiler eğrisi; `archive/` içinde en küçük cost; en iyi genomda yapısal değişim (orbit sayısı serbest mi, endgame derinleşti mi).
- [ ] **Step 3:** `cost < 620` çıktıysa → Görev 8 (rekor prosedürü). Çıkmadıysa plato analizi → Görev 7 kararını kullanıcıya sun.

---

### Görev 7 (koşullu — plato olursa): Thinking-açık Opus deneyi

Bağlam: dünkü thinking-kapalı ayarı ölçülmüş bir "thinking zararlı" bulgusu DEĞİL, 20k max_tokens'ta bütçenin reasoning'e gidip content'in boş kalmasına karşı tamirdi. Doğru deney: thinking açık + content'e yer bırakan bütçe.

- [ ] **Step 1:** `C:\kt\upwork\cli-api\config.yaml`'ı `config.thinking.yaml` olarak kopyala; içindeki `payload.override` bloğundan `thinking: {type: disabled}` dayatmasını KALDIR (blok tamamen silinmez — `context_management` filtresi kalır, CLAUDE.md §0 8317-tamir notlarını oku). Yeni compose servisi: 8320 portu (`docker-compose.thinking.yml`, mevcut compose dosyalarından kopyala, config volume'ü `config.thinking.yaml`).
- [ ] **Step 2:** Replika istekle doğrula: 51k karakterlik prompt + `max_tokens: 64000` → `finish_reason: stop`, content dolu. (CLAUDE.md §0'daki replika deseni.)
- [ ] **Step 3:** `config.covering.v32.thinking.yaml`: v32 config'in kopyası; `llm.models` yalnız `claude-opus-5` (api_base 8320), `max_tokens: 64000`, `diff_based_evolution: false`. 10 iterlik deney dilimi; content-boş oranı ve skor kıpırtısı ölç; kullanıcıya raporla.

---

### Görev 8 (koşullu — cost < 620): Rekor prosedürü

- [ ] **Step 1:** Arşivdeki çözümü kesin doğrula: `python data/covering/results/C28-9-3-cost56/verify_cover.py 32 8 4 <arsiv-dosyasi>` → `GECERLI COVERING`.
- [ ] **Step 2:** coveringrepository.com çaprazı (curl 403 verir; uygulama içi Browser pane kullan): ana sayfa → form `ddlV=32, ddlK=8, ddlT=4, ddlM=4` → `cph_btnSearch` — dünkü JS yöntemi CLAUDE.md/plan geçmişinde. Güncel değer hâlâ 620 mi kontrol et.
- [ ] **Step 3:** Sertifika paketi: `data/covering/results/C32-8-4-costXXX/` (solution.txt + verify_cover.py kopyası + README — C28 paketindeki README şablonunu kullan, "EŞİTLEME" yerine "İYİLEŞTİRME" dili ancak Step 2 teyidiyle).
- [ ] **Step 4:** KULLANICIYA SUN: sonuç + coveringrepository.com'a gönderim seçeneği. **Gönderim = dış paylaşım = kullanıcı onayı olmadan YAPILMAZ.**

---

## Kapanış

- [ ] Her görev sonunda `CLAUDE.md` Faz P5'e tek paragraf kayıt düş (fiilen çalışan komutlarla).
- [ ] Koşu sonuçları ne olursa olsun: negatif bulgular da rapora girer (güvenilirlik oradan gelir).
